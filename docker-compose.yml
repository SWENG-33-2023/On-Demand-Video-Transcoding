version: "3.9"
services:
  back-end:
    container_name: back-end
    build: back-end
    volumes:
      - assets:/front-end/assets
    ports:
      - "4000:4000"
  redis:
    image: "redis:alpine"
  front-end:
    container_name: front-end
    build: front-end
    volumes: 
      - assets:/front-end/output-videos
    ports:
      - "3000:3000"
volumes:
  assets:
